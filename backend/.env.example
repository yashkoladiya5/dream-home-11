# App configuration
PORT=3000
NODE_ENV=development

# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=dream_home_11

# Redis configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT Configuration
JWT_SECRET=super-secret-jwt-key-for-dream-home-11
JWT_EXPIRATION=7d

# Firebase Configuration
# Path to your Firebase service account JSON key file (e.g., ./firebase-service-account.json)
FIREBASE_SERVICE_ACCOUNT_PATH=
# Enables mock token verification bypass for local testing ('true' or 'false')
ENABLE_MOCK_AUTH=true
